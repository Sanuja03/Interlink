# Supabase Postgres password for user postgres.zyvhemluugmpciqnpygo
DB_PASSWORD=

# OpenAI API key used by the RAG endpoints (openai.api.key)
OPENAPI_KEY=

# Supabase service role key (supabase.service.key)
SERVICE_KEY=

# Comma-separated origins allowed by CorsConfig. Must include wherever the
# frontend container is published, e.g. http://localhost:8081.
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://localhost:5173
